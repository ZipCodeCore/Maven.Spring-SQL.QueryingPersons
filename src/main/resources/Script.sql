
  	Command


DELETE FROM MOVIES
WHERE RATING = 'R';

SELECT RATING, COUNT(*)
FROM MOVIES
GROUP BY RATING
HAVING COUNT(*) > 1;

SELECT AVG(IMDB_SCORE), MAX(IMDB_SCORE), MIN(IMDB_SCORE)
FROM MOVIES
GROUP BY RATING;

SELECT ID, RATING
FROM MOVIES
WHERE GENRE = 'Horror'
OR GENRE = 'Documentary';

UPDATE MOVIES
SET RATING = 'R'
WHERE TITLE  = 'Starship Troopers';

SELECT AVG(RUNTIME)
FROM MOVIES
WHERE IMDB_SCORE < 7.5
GROUP BY GENRE

Edit	SELECT * FROM MOVIES

Edit	SELECT TITLE
FROM MOVIES
WHERE RATING = 'G'
OR RATING = 'PG'

Edit	SELECT TITLE
FROM MOVIES
WHERE IMDB_SCORE >= 6.5

Edit	SELECT TITLE
FROM MOVIES
WHERE  IMDB_SCORE = 6.5

Edit	SELECT TITLE
FROM MOVIES
WHERE GENRE = 'Sci-Fi'

